name: "Delete Tag"
description: "Deletes an existing Tag via GitHub Actions"
author: "DCx7C5"

branding:
  icon: "git-branch"
  color: "purple"


inputs:
  tag_name:
    required: true
    description: "The tag name/version to delete"
  delete_release:
    required: false
    default: 'false'
    description: "Whether to also delete the associated release (if it exists) - requires gh CLI and appropriate permissions"


outputs:
  deleted:
    description: "Whether the tag was successfully deleted"
    value: ${{ steps.tag_delete.outputs.deleted }}
  tag_existed:
    description: "Whether the tag existed locally before deletion"
    value: ${{ steps.tag_delete.outputs.tag_existed }}
  pushed:
    description: "Whether the deletion was pushed to remote"
    value: ${{ steps.tag_delete.outputs.pushed }}

runs:
  using: composite
  steps:

    - name: Delete release
      id: rel_delete
      if: inputs.delete_release == 'true'
      shell: bash
      env:
        RELEASE_TAG: ${{ inputs.tag_name }}
        GH_TOKEN: ${{ inputs.github_token || env.GITHUB_TOKEN || github.token }}
      run: |
        set -euo pipefail

        # Validate input
        if [ -z "$RELEASE_TAG" ]; then
          echo "::error::release_tag input is required and cannot be empty"
          exit 1
        fi

        # Get the release ID for the given tag
        RELEASE_ID=$(gh release view "$RELEASE_TAG" --json id --jq '.id' 2>/dev/null || echo "")

        if [ -n "$RELEASE_ID" ]; then
          echo "::notice::Deleting release for tag '$RELEASE_TAG' (ID: $RELEASE_ID)"

          if gh release delete "$RELEASE_TAG" --yes 2>&1; then
            echo "::notice::Release '$RELEASE_TAG' deleted successfully"
            echo "deleted=true" >> "$GITHUB_OUTPUT"
            echo "release_id=$RELEASE_ID" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Failed to delete release '$RELEASE_TAG'"
            exit 1
          fi
        else
          echo "::warning::Release for tag '$RELEASE_TAG' not found - nothing to delete"
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          echo "release_id=" >> "$GITHUB_OUTPUT"
        fi

    - name: Delete tag
      id: tag_delete
      shell: bash
      env:
        TAG_NAME: ${{ inputs.tag_name }}
      run: |
        set -euo pipefail
        
        # Validate input
        if [ -z "$TAG_NAME" ]; then
          echo "::error::tag_name input is required and cannot be empty"
          exit 1
        fi
        
        TAG_EXISTS=false
        PUSH_SUCCESS=false
        
        # Check if tag exists locally
        if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
          TAG_EXISTS=true
          echo "::notice::Tag '$TAG_NAME' found locally - deleting..."
          
          if git tag --delete "$TAG_NAME"; then
            echo "::notice::Local tag '$TAG_NAME' deleted successfully"
          else
            echo "::error::Failed to delete local tag '$TAG_NAME'"
            exit 1
          fi
        else
          echo "::notice::Tag '$TAG_NAME' does not exist locally"
        fi
        
        # Try to delete from remote (don't fail if tag doesn't exist remotely)
        if git push origin --delete "$TAG_NAME" 2>&1; then
          echo "::notice::Remote tag '$TAG_NAME' deleted successfully"
          PUSH_SUCCESS=true
        else
          # Don't fail here - remote deletion is best-effort
          if [ "$TAG_EXISTS" = true ]; then
            echo "::warning::Local tag was deleted but remote deletion failed (tag may not exist on remote)"
            PUSH_SUCCESS=false
          else
            echo "::notice::Tag '$TAG_NAME' does not exist remotely - nothing to delete"
            PUSH_SUCCESS=false
          fi
        fi
        
        # Set outputs - Action succeeds if ANYTHING was deleted
        if [ "$TAG_EXISTS" = true ] || [ "$PUSH_SUCCESS" = true ]; then
          echo "deleted=true" >> "$GITHUB_OUTPUT"
          echo "::notice::Tag deletion completed successfully"
        else
          echo "deleted=false" >> "$GITHUB_OUTPUT"
          echo "::notice::Tag '$TAG_NAME' did not exist locally or remotely - no action taken"
        fi
        
        echo "tag_existed=$TAG_EXISTS" >> "$GITHUB_OUTPUT"
        echo "pushed=$PUSH_SUCCESS" >> "$GITHUB_OUTPUT"
